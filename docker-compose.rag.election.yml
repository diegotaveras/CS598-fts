services:
  node1:
    environment:
      RAG_FORCE_LEADER_ID: ""
      RAG_ROUTING_TREE_EXPECTED_USERS: "4"
      RAG_LEASE_DURATION_SECONDS: "${RAG_LEASE_DURATION_SECONDS:-15}"
      RAG_LEADER_MONITOR_SECONDS: "${RAG_LEADER_MONITOR_SECONDS:-2}"

  node2:
    environment:
      RAG_FORCE_LEADER_ID: ""
      RAG_ROUTING_TREE_EXPECTED_USERS: "4"
      RAG_LEASE_DURATION_SECONDS: "${RAG_LEASE_DURATION_SECONDS:-15}"
      RAG_LEADER_MONITOR_SECONDS: "${RAG_LEADER_MONITOR_SECONDS:-2}"

  node3:
    environment:
      RAG_FORCE_LEADER_ID: ""
      RAG_ROUTING_TREE_EXPECTED_USERS: "4"
      RAG_LEASE_DURATION_SECONDS: "${RAG_LEASE_DURATION_SECONDS:-15}"
      RAG_LEADER_MONITOR_SECONDS: "${RAG_LEADER_MONITOR_SECONDS:-2}"

  node4:
    environment:
      RAG_FORCE_LEADER_ID: ""
      RAG_ROUTING_TREE_EXPECTED_USERS: "4"
      RAG_LEASE_DURATION_SECONDS: "${RAG_LEASE_DURATION_SECONDS:-15}"
      RAG_LEADER_MONITOR_SECONDS: "${RAG_LEADER_MONITOR_SECONDS:-2}"
